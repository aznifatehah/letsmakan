class Product {
  String id, name, price, quantity, type, date, sold;
  Product({
    this.id,
    this.name,
    this.price,
    this.quantity,
    this.type,
    this.date,
    this.sold,
  });
}